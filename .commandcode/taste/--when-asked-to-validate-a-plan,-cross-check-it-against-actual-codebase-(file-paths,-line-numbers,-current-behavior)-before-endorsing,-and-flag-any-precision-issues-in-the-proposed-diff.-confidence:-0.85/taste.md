# - When asked to validate a plan, cross-check it against actual codebase (file paths, line numbers, current behavior) before endorsing, and flag any precision issues in the proposed diff. Confidence: 0.85
- When asked to validate a plan, cross-check it against actual codebase (file paths, line numbers, current behavior) before endorsing, and flag any precision issues in the proposed diff. Confidence: 0.85
- When asking multi-question decisions about a plan, mark sensible defaults as "(Recommended)" rather than presenting neutral options. Confidence: 0.8
- Prefers that bug fixes ship with both manual verification steps and an automated unit test (vitest) covering the core logic. Confidence: 0.85
- Plan artifacts go in `~/.commandcode/plans/` with kebab-case filenames (e.g. `fix-conversation-leak.md`). Confidence: 0.9
- When a plan touches non-trivial logic, prefer co-locating new middleware/helpers in the existing top-level wiring file (e.g. `bot.ts`) rather than deep inside feature modules, unless there's a strong co-location reason. Confidence: 0.6
- Prefers user-facing bot copy to be Persian-first with HTML parse mode for replies. Confidence: 0.85
- Prefers every Telegram reply to be wrapped with `.catch(() => {})` to swallow errors silently. Confidence: 0.7
- Prefers scoped changes: when shipping a fix, explicitly mark pre-existing test/type failures as out-of-scope rather than fixing them in the same diff. Confidence: 0.8
- Prefers the validation/deploy flow order: cross-check diff against code → run tests → run tsc → dry deploy → live deploy, with each step reported separately. Confidence: 0.75
- When presenting a deploy request, supplies an upfront architecture summary (stack, entry points, bindings, feature layout, critical conventions) so the agent has full context before acting. Confidence: 0.8
- When asking for validation+deploy, lists "Manual verification still pending" steps separately from automated test status, keeping the two concerns distinct. Confidence: 0.75
