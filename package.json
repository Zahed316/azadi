{
  "name": "azadi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "deploy": "npm exec -- wrangler deploy",
    "deploy:dry": "npm exec -- wrangler deploy --dry-run --outdir ./wrangler-dry",
    "setup:webhook": "TELEGRAM_BOT_TOKEN=$(grep '^TELEGRAM_BOT_TOKEN=' ~/.env | cut -d= -f2-) SECRET_TOKEN=$(grep '^SECRET_TOKEN=' ~/.env | cut -d= -f2-) && [ -n \"$TELEGRAM_BOT_TOKEN\" ] && [ -n \"$SECRET_TOKEN\" ] && curl -s -X POST \"https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/setWebhook\" -d \"url=https://azadi-coffee-bot.zahedrastgar316.workers.dev/webhook&secret_token=${SECRET_TOKEN}\" | python3 -m json.tool || { echo \"Missing TELEGRAM_BOT_TOKEN or SECRET_TOKEN in ~/.env\" >&2; exit 1; }"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@grammyjs/auto-retry": "^2.0.2",
    "@grammyjs/conversations": "^2.1.1",
    "@grammyjs/menu": "^1.3.1",
    "@grammyjs/parse-mode": "^2.3.0",
    "@grammyjs/router": "^2.0.0",
    "drizzle-orm": "^0.45.2",
    "grammy": "^1.45.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260731.1",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.118.0"
  }
}
