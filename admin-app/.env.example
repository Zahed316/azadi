# API base URL for the admin Mini App.
# Override when pointing at a local Worker (e.g. wrangler dev).
# VITE_API_BASE=http://localhost:8787/api
VITE_API_BASE=https://azadi-coffee-bot.zahedrastgar316.workers.dev/api
